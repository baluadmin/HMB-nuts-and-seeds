kivy
requests
```[cite: 3]

Updated `buildozer.spec` requirements line:
```ini
requirements = python3,kivy,requests,urllib3,certifi,idna,charset_normalizer
```[cite: 1]

Try running `buildozer -v android clean` followed by `buildozer -v android debug` with these changes to clear out the compilation blockage.
